#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running format check on staged files (fast)..."
# Format staged files only — faster and safe for pre-commit
yarn format:staged || {
	echo "Formatting failed. Run 'yarn format' to fix issues.";
	exit 1;
}

echo "✅ Pre-commit checks passed (build skipped)."
 